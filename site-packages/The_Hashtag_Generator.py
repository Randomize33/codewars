def generate_hashtag(s):
    list_text = s.split(' ')
    hashtag=''.join(list(map(lambda x: x.capitalize(), list_text)))
    return False if len(hashtag)==0 or len(hashtag)>139 else '#'+hashtag.replace(' ','')


text='Codewars Is Nice'
result=generate_hashtag(text)
print(result)